# 📁 Структура проекта MADN Studio

## Общая концепция

Проект организован по модульному принципу с четким разделением ответственности. Каждая директория имеет свое назначение и содержит файлы определенного типа.

## 📂 Детальное описание директорий

### `src/app/` - Точка входа приложения

Содержит основной компонент приложения и точку входа React.

```
app/
├── App.tsx          # Главный компонент с роутингом и состоянием
└── main.tsx         # React DOM рендеринг
```

**Ответственность:**
- Управление глобальным состоянием
- Роутинг между страницами
- Темы и общие настройки

---

### `src/pages/` - Страницы приложения

Полноценные страницы, которые рендерятся как отдельные роуты.

```
pages/
├── TeamPage.tsx              # Страница команды
├── WorkPage.tsx              # Портфолио с фильтрами
├── ProjectDetailsPage.tsx    # Детали проекта
├── CareersPage.tsx          # Вакансии
├── ContactPage.tsx          # Форма контактов
├── LegalPage.tsx            # Privacy, Terms, Cookies
└── index.ts                 # Экспорт всех страниц
```

**Ответственность:**
- Композиция UI компонентов
- Бизнес-логика страницы
- Взаимодействие с данными

---

### `src/components/` - Переиспользуемые компоненты

#### `layout/` - Компоненты макета

Компоненты, которые формируют структуру страницы.

```
layout/
├── Header.tsx       # Навигация, лого, темы
├── Footer.tsx       # Футер с ссылками
└── index.ts         # Экспорт компонентов
```

#### `ui/` - UI компоненты

Переиспользуемые UI элементы без бизнес-логики.

```
ui/
├── Button.tsx                  # Кнопка с вариантами
├── ChatWidget.tsx             # AI чат-бот
├── DecryptedText.tsx          # Анимация текста
├── SpidermanBackground.tsx    # Canvas анимация
└── index.ts                   # Экспорт компонентов
```

**Принципы:**
- Независимость от бизнес-логики
- Принимают данные через props
- Могут быть использованы в любом месте

---

### `src/config/` - Конфигурация и данные

Централизованное хранение статических данных и настроек.

```
config/
├── navigation.ts    # Пункты меню навигации
├── projects.ts      # Данные о проектах/кейсах
├── services.ts      # Услуги студии
├── team.ts          # Члены команды
└── index.ts         # Реэкспорт всех конфигов
```

**Преимущества:**
- ✅ Легко обновлять данные
- ✅ Централизованное место для контента
- ✅ Не нужно лезть в компоненты

**Как обновлять данные:**

```typescript
// config/projects.ts
export const PROJECTS: Project[] = [
  {
    id: '9',
    title: 'Новый проект',
    category: 'Web3',
    imageUrl: 'https://...',
    year: '2026',
    description: 'Описание проекта',
    stack: ['React', 'Web3.js']
  }
];
```

---

### `src/types/` - TypeScript типы

Определения типов для всего приложения.

```
types/
└── index.ts         # Все интерфейсы и типы
```

**Основные типы:**
```typescript
interface Project { ... }
interface Service { ... }
interface TeamMember { ... }
interface ChatMessage { ... }
interface NavItem { ... }
```

---

### `src/services/` - Внешние сервисы

Логика работы с внешними API и сервисами.

```
services/
└── gemini.ts        # Google Gemini AI для чат-бота
```

**Ответственность:**
- HTTP запросы
- Работа с API
- Форматирование данных

---

### `src/assets/` - Статические ресурсы

Изображения, шрифты, иконки и другие медиа.

```
assets/
├── images/
├── fonts/
└── icons/
```

---

## 🎯 Алиасы импортов

### Настройка

Алиасы настроены в двух местах:
1. `vite.config.ts` - для Vite bundler
2. `tsconfig.json` - для TypeScript

### Использование

```typescript
// ❌ Плохо: относительные пути
import Button from '../../../components/ui/Button';
import { PROJECTS } from '../../../../config/projects';

// ✅ Хорошо: алиасы
import Button from '@ui/Button';
import { PROJECTS } from '@config';
```

### Доступные алиасы

| Алиас | Путь | Использование |
|-------|------|---------------|
| `@/*` | `src/*` | Любой файл из src |
| `@app/*` | `src/app/*` | App.tsx, main.tsx |
| `@pages/*` | `src/pages/*` | Страницы |
| `@components/*` | `src/components/*` | Любые компоненты |
| `@ui/*` | `src/components/ui/*` | UI компоненты |
| `@layout/*` | `src/components/layout/*` | Header, Footer |
| `@config/*` | `src/config/*` | Данные и настройки |
| `@types/*` | `src/types/*` | TypeScript типы |
| `@services/*` | `src/services/*` | API сервисы |
| `@assets/*` | `src/assets/*` | Статика |

---

## 📋 Принципы организации кода

### 1. **Разделение ответственности**
- Страницы отвечают за бизнес-логику
- UI компоненты - чисто визуальные
- Сервисы - работа с API
- Config - данные

### 2. **DRY (Don't Repeat Yourself)**
- Общие компоненты в `ui/`
- Данные в `config/`
- Типы в `types/`

### 3. **Масштабируемость**
- Легко добавить новую страницу
- Легко добавить новый компонент
- Легко обновить данные

### 4. **Читаемость**
- Понятная структура папок
- Короткие импорты через алиасы
- Именование файлов в PascalCase

---

## 🚀 Добавление нового функционала

### Добавить новую страницу

1. Создать файл в `src/pages/NewPage.tsx`
2. Добавить экспорт в `src/pages/index.ts`
3. Добавить импорт в `src/app/App.tsx`
4. Добавить роут в логику навигации

### Добавить новый UI компонент

1. Создать файл в `src/components/ui/NewComponent.tsx`
2. Добавить экспорт в `src/components/ui/index.ts`
3. Использовать через `import { NewComponent } from '@ui'`

### Обновить данные

Просто отредактировать нужный файл в `src/config/`:
- Проекты → `projects.ts`
- Команда → `team.ts`
- Услуги → `services.ts`

---

## 🔍 Поиск по проекту

### Где искать что?

| Что ищу | Где искать |
|---------|------------|
| Роутинг, навигация | `src/app/App.tsx` |
| UI кнопки, виджеты | `src/components/ui/` |
| Хедер, футер | `src/components/layout/` |
| Страницы | `src/pages/` |
| Данные о проектах | `src/config/projects.ts` |
| Данные о команде | `src/config/team.ts` |
| API чат-бота | `src/services/gemini.ts` |
| Типы | `src/types/index.ts` |

---

## 💡 Best Practices

### Импорты

✅ **Хорошо:**
```typescript
import { Button, ChatWidget } from '@ui';
import { PROJECTS, SERVICES } from '@config';
import { Project } from '@types';
```

❌ **Плохо:**
```typescript
import Button from '../../../components/ui/Button';
import ChatWidget from '../../../components/ui/ChatWidget';
```

### Компоненты

✅ **Хорошо:**
```typescript
// Простой, переиспользуемый компонент
const Button = ({ variant, children, ...props }) => {
  return <button className={styles[variant]} {...props}>{children}</button>
}
```

❌ **Плохо:**
```typescript
// Компонент с жестко закодированными данными
const Button = () => {
  const projects = [...]; // ❌ Данные должны быть в config
  return <button>Кнопка</button>
}
```

### Типизация

✅ **Хорошо:**
```typescript
interface ButtonProps {
  variant: 'primary' | 'secondary';
  icon?: boolean;
  onClick?: () => void;
}

const Button: React.FC<ButtonProps> = ({ variant, icon, onClick }) => { ... }
```

---

## 📝 Итог

Эта структура обеспечивает:
- ✅ Понятную организацию кода
- ✅ Легкость в поддержке и расширении
- ✅ Простоту обновления данных
- ✅ Хорошую читаемость
- ✅ Масштабируемость проекта


